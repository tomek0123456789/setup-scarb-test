name: My workflow
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: software-mansion/setup-scarb@fce8e88c42cf82ed3968958417060a23386aad3d
        with:
          scarb-version: "2.3.1"
      - name: cwd
        run: scarb fmt --check && ls -la
        working-directory: ./crates/addition
      - name: hmm
        run: ls -la
