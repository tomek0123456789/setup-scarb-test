name: My workflow
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: software-mansion/setup-scarb@302b4829ac7d9cd6556ea3f138cb07b046fefcd4
        with:
          scarb-version: "2.3.1"
      - run: scarb fmt --check
#      - name: cwd
#        run: scarb fmt --check && ls -la
#        working-directory: ./crates/addition
#      - name: hmm
#        run: ls -la
