name: My workflow
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: cwd
        run: scarb fmt --check && ls -la
        working-directory: ./crates/addition
#      - uses: actions/checkout@v3
      - uses: software-mansion/setup-scarb@test-cwd
        with:
          scarb-version: "2.3.1"
#      - run: scarb fmt --check
#      - name: hmm
#        run: ls -la
