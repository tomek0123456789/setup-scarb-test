name: My workflow
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: software-mansion/setup-scarb@04293aaac73b65d6135bfce8752b031832b6b50e
        with:
          scarb-version: "2.3.1"
      - run: scarb fmt --check
#      - name: cwd
#        run: scarb fmt --check && ls -la
#        working-directory: ./crates/addition
#      - name: hmm
#        run: ls -la
