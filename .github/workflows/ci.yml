name: My workflow
on:
  push:
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: software-mansion/setup-scarb@28d0644e301a02fe1a09d99416e8f8b6afb33df2
        with:
          scarb-version: "2.3.1"
      - run: scarb fmt --check
#      - name: cwd
#        run: scarb fmt --check && ls -la
#        working-directory: ./crates/addition
#      - name: hmm
#        run: ls -la
